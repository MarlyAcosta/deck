# Preconditions: Adaptive Memory Capture Policy Markdown Lists

- The implementation WIP in `packages/core/src/memory/capture-eligibility.ts`, `packages/core/src/memory/capture-eligibility.test.ts`, and `packages/adapter-supermemory/src/runtime.test.ts` predates this change ID.
- This creation step is authorized to write only `openspec/changes/fix-adaptive-memory-capture-policy-markdown-lists/**` and any unavoidable shared canonical registry record. No shared registry write is currently required.
- The three WIP source/test paths are read-only during this step and require separate authorization before any future edit.
- `openspec/changes/fix-opencode-automatic-memory-same-turn/working-brief.md` is provisional context only and remains unchanged.
- `openspec/changes/adaptive-memory-observability-receipts/**`, Serena, `.codex`, `.bun-cache`, generated assets, build info, and every unrelated path remain excluded.
- No Setup, Apply, TDD RED, Verify, Review, or acceptance activity may be backdated or attributed retroactively to this change.
- Future Apply begins with inspection and adoption of the existing WIP. If any hunk falls outside this specification or any source edit is needed, work MUST stop until path-specific authority is granted.
- Future verification uses synthetic content and fake transport. It MUST make no live provider call and MUST NOT require a live canary.
- No test, build, release suite, archive, stage, commit, restore, discard, push, tag, release, or publication is authorized by this creation step.
