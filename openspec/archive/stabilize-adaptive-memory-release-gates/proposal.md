# Proposal: Stabilize Adaptive Memory Release Gates

## Intent

Resolve the remaining release-readiness failures for the current Adaptive Memory candidate in one coordinated, causally bounded stabilization pass.

## Scope

- Confirm current Adaptive Memory agent/session/skill bundle content is intentional and equivalent, then refresh only the canonical byte-exact baseline if stale.
- Restore the provider-neutral Core boundary for managed project-memory recall without weakening the purity audit or secret screening.
- Remove nondeterminism from the Codex RunnerAdapter timeout test through the correct injectable process boundary or production correction supported by evidence.
- Remove the fixed-wall-clock dependency from mounted TUI Codex discovery verification while preserving real discovery semantics.
- Produce focused RED/GREEN evidence, independent Review, one final normative BROAD gate, and conditional Archive evidence.

## Non-goals

- No version or changelog modification.
- No generated-asset hand editing.
- No provider/live calls, canary installation, release preparation output, publication, or Git mutation beyond ordinary worktree edits.
- No repair of unrelated historical OpenSpec artifacts or existing WIP.
- No reopening of already completed Adaptive Memory observability or capture-policy behavior.

## Risks and rollback

- Secret screening must not regress when removing a provider-specific literal from Core.
- Test determinism must not conceal actual process or discovery failures.
- Bundle baseline refresh must follow content verification, not force production text toward an obsolete hash.

Rollback is a normal follow-up edit or revert of this candidate's owned files only. Destructive Git commands are forbidden.
