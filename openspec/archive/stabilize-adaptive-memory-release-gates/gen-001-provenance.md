# GEN-001 — Canonical Generated Asset Closure

## Current disposition and authority

Technical resolution and focused Verify: **PASS**. Independent `deck-quality` Review: **GO**, zero Critical/Required; **readiness READY**. See [gen-001-review.md](gen-001-review.md) for the new Optional finding and preserved failed reviewer attempt. Conditional Archive is authorized. Version/CHANGELOG preparation and all Git/publication effects remain unauthorized.

The preceding five-path Review is historical evidence only for its original subject. Its scope is explicitly **invalidated for the expanded generated/tooling candidate**. No equivalence to the unavailable historical OpenCode `ab7a2f439494fe7c5a4c36de1f4ceb6a712d1c29f7b90ef03a6af7286aef20f1` bytes is claimed. The new Review must bind the complete current source closure, generated bytes and generator, rather than inherit that digest.

## Identity and ownership

HEAD remains `dd596899b649ad9a0cda00b1d85749f8a281bab5`; index empty. This continuation began with 27 tracked modified paths and 48 individually enumerated untracked files. The immutable baseline was captured before modification at `/tmp/opencode/gen-001/baseline-authoritative-20260907T143210Z-dd596899b649`.

The expanded nine-path subject is recorded in [candidate.json](gen-001-evidence/candidate.json): SHA-256 `64a81e9e30b3d7e3139ea11e0384ea983da573a9f4e5f99dd07e531489d5a7d9`. It retains the five original stabilization source/test paths and adds:

- `packages/adapter-opencode/assets/opencode/plugins/developer-team-execution.generated.js`: canonical replacement of pre-existing generated WIP, expressly adopted by stabilization.
- `packages/adapter-pi/assets/pi/extensions/developer-team-execution.generated.js`: canonical adoption; final bytes equal HEAD, not a Git discard.
- `scripts/generate-runner-execution-assets.ts`: toolchain-drift prevention.
- `scripts/generate-runner-execution-assets.test.ts`: permanent regression coverage.

OpenCode TypeScript and all other pre-existing source/test WIP remain byte-unchanged in this continuation. Generated Codex is a control only and was not written in the workspace. The protected documents, `.serena`, `.codex`, `.bun-cache`, package versions, lockfile and CHANGELOG are excluded from modification.

## Exact producer and dependency closure

The approved `bun run build` executes `bench:memory`, `verify:supermemory-compiled`, then `scripts/build-binaries.ts`. Build-binaries invokes `bun run scripts/generate-runner-execution-assets.ts` before compilation. The generator writes each asset using:

```text
<executing Bun> build <entry.ts> --target=bun --format=esm --minify --outfile=<entry.generated.js>
```

It prefixes a generated notice and `// source-sha256:<entry digest>`. The latter proves **entry provenance only**, not transitive freshness or equivalence. Generation used isolated mirrors and sanitized PATH; after repair, child builds use `process.execPath`. Mirrors generated all three canonical entries, but only the two authorized outputs were adopted. Codex control digest stayed `66361f5f653ab4ea944e9c9f8b8428e2131a825eda913c3b5ae563e867498863`.

| Adapter | Entry SHA-256 | Closure inputs | Path/hash manifest SHA-256 |
|---|---|---:|---|
| OpenCode | `31e81e932d71397b7c4177d9650c2640a31478607508b9450f087ebc9f242c59` | 155 | `948f486d620a14e8943a9b0f718e0b3da760ace269a05eef2457ecc69381041c` |
| Pi | `0285e8cca53df1f11cc90a255bafd6396001b91bb9426469e849c761143f7d26` | 154 | `55229a7bc6b1fb009138e53143729e7e1da27b979c3bfa04de03a0455943d7a1` |

Full path/hash manifests and Bun metafile import edges are persisted under [gen-001-evidence](gen-001-evidence/evidence-index.json). They include repository modules and installed `yaml`. Runtime externals are Bun/Node builtins `buffer`, `crypto`, `fs`, `fs/promises`, `path`, and `process`, not omitted third-party files. Metafile output equals canonical output without the two header lines. Configuration and dependencies were held constant; no dependency installation or lockfile edit was used.

Graph project `deck`, generation `2026-08-20T17:25:00Z`, fast index: scripts, assets and many tests are excluded by design. Direct source, diffs, metafiles, hashes and tests are authoritative; no graph-completeness claim is made.

## Bun provenance and determinism

Bun 1.3.12 came from the official [Bun v1.3.12 release](https://github.com/oven-sh/bun/releases/tag/bun-v1.3.12), GitHub Releases API and release asset, retrieved 2026-09-07T14:34:31Z, published 2026-04-10T03:04:46Z. The official `bun-linux-x64.zip` digest and actual download both equal `11dc3ee11bc1695e149737c6ca3d5619302cf4346e6b8a6ec7988967ef01ddc5`; extracted revision `1.3.12+700fc117a`. Local comparison: `1.4.0+34cbb9a40`. Host: Linux x64. No global replacement occurred.

| Environment | Repeats | OpenCode output SHA-256 | Pi output SHA-256 |
|---|---:|---|---|
| Original generator, Bun 1.3.12 | 2 identical | `b82a7f07014360fe542f075bc7315b62c26d1129be6316d3c672b83455f9aa82` | `b64918dcc24ecec0c8c119da6ef9939c66dad7a9f71aa58910200ac0450ddb96` |
| Original generator, Bun 1.4.0 | 2 identical | `f1a1f06b15ad0b643c4b701117544be3d37df36b6d8620412bfd59e76b9510bf` | `5edde47a3a38b4f7a965885b0f145308df0612a5523a9f1b3dae82d30a5f6b32` |
| Repaired generator, Bun 1.3.12 | 2 identical | `b82a7f07014360fe542f075bc7315b62c26d1129be6316d3c672b83455f9aa82` | `b64918dcc24ecec0c8c119da6ef9939c66dad7a9f71aa58910200ac0450ddb96` |
| Repaired generator, Bun 1.4.0 | rejected | No output mutation | No output mutation |

## Cause and semantic disposition

**Case B applies:** deterministic but different toolchain emission from the same closure, not nondeterminism or a later entry edit during the approved build.

Current-closure unminified outputs differ in precisely 71 CommonJS wrapper emissions (`__commonJS((exports) => {` versus `__commonJS(function(exports) {`) and one export-specifier ordering change per adapter. Normalizing only those classes yields identical unminified bytes, with zero unclassified differences. Different wrapper emission feeds minified local-name allocation and explains the rename-heavy diff. This is combined with dynamic source/generated behavior and generated install/execution tests; exports or entry hashes alone are not the equivalence argument.

- **Pi:** its entire closure is unchanged from HEAD. Bun 1.3.12 reproduces its HEAD bundle exactly. The intake-to-final diff removes only the 1.4.0 emission drift; final HEAD diff is empty.
- **OpenCode:** two closure files differ from HEAD: the entry adds logical-turn/session tracking and metadata-only hashed injection acknowledgments; `managed-project-memory-recall.ts` broadens provider-neutral credential/header rejection. These reviewed source changes explain its HEAD-to-final semantic delta. No new closure path or dependency version explains the difference. Intake 1.4.0-to-adopted 1.3.12 is toolchain-only. HEAD source plus Bun 1.3.12 reproduces HEAD OpenCode digest `e6eae7da17c85c8bf53150944af06191dce786ec09f3f5cb82bfa201c2c8a7eb`.

Full HEAD-to-intake, intake-to-adopted and HEAD-to-adopted patches are persisted per bundle. Unavailable historical `ab7a...` bytes are not used for readiness; current full-closure verification supersedes that byte subject without deleting its evidence.

## Drift prevention

The generator reads a single consistent exact Bun pin from `.github/workflows/release.yml`, checks the executing binary before output writes, rejects mismatches with isolated-toolchain guidance, and uses the same absolute executable for child builds. It neither downloads Bun nor updates a global toolchain. Regression tests cover workflow binding, wrong-version zero mutation for all three outputs, matching-version generation, and fake-PATH resistance.

## Focused Verify

| Evidence | Result |
|---|---|
| Implementation: seven source/generated/install/Adaptive Memory files under each Bun version | 225 pass, 0 fail per version |
| Generator TDD | Recorded RED before implementation; 3 tests PASS under each version |
| Focused strict TypeScript over generator and test | PASS under both versions |
| Lead final Verify, Bun 1.3.12: same seven files plus generator regression | **228 pass, 0 fail, 1,260 expectations, 8 files** |
| Dynamic source/generated probe | PASS: recall contract, extra-argument rejection, token/body separation, logical turns, hashed injection acknowledgments, Pi role mapping |
| Adaptive Memory semantics | Exact `{ query }`, no scope/provider/limit/secret arguments, advisory authority and focused recall guidance preserved; installer prompt tests green |
| Privacy scan | No literal workspace/temp paths, project scope, stale provider tools or test-secret sentinels in bundles; runtime variable names are not credential values |
| Focused OpenSpec validation | Three eligible changes and workflow canonical archived change: 0 errors, 0 warnings |
| `git diff --check` | PASS |

Lead logs: `/tmp/opencode/gen-001/lead-verify/`. Implementation summaries/logs and behavioral probe are persisted in `gen-001-evidence/`.

## Causal BROAD disposition

Final bytes differ from the approved Bun 1.4.0 build, so compiled-runtime smoke was repeated under isolated Bun 1.3.12: **PASS**, including Linux x64 extracted-archive execution and compile-only other targets. The tooling change received isolated `build-binaries.ts --dry-run`: PASS with pinned Bun; expected fail-before-bundle-write with 1.4.0. No workspace third generated asset was touched.

Full suite, benchmark, release-descriptor and prepare-release tests were **not repeated**: no runtime TypeScript/dependency behavior changed, and affected generated routes have focused and compiled evidence. Earlier normative results remain inherited evidence, not new executions. The previous response-loss/full-suite repetition deviation remains disclosed in `release-readiness.md` and tasks; it is not falsely relabeled as one historical invocation.

## Archive boundary

Final closure: all three named ready changes were archived and post-validated with zero errors/warnings. See [archive-report.md](archive-report.md). The following paragraph retains the pre-move eligibility and mechanism decision.

Only the observability, capture-policy and stabilization changes are eligible. The workflow's `canonical-supermemory-conversation-memory` is already archived and validates by `--change`; it remains untouched. No external `openspec` binary is available. The repository's canonical Archive mechanism is the Lead-owned report/verified move/registry transition in `packages/core/src/teams/developer/archive-content.ts`, not an invented CLI command.
