# GEN-001 Independent Review

## Verdict

**GO — 0 Critical, 0 Required, 1 new Optional.**

Reviewer: `deck-quality`, independent of `deck-apply-deep`. Lead reconciled the completed review on 2026-09-07 after the user resumed the interrupted review. Subject: `sha256:64a81e9e30b3d7e3139ea11e0384ea983da573a9f4e5f99dd07e531489d5a7d9`; HEAD `dd596899b649ad9a0cda00b1d85749f8a281bab5`. All nine hashes independently matched. Empty index and diff checking passed. This verdict covers the expanded subject, not a reuse of the old five-path GO.

## Verified axes

- All 155 OpenCode and 154 Pi closure hashes match current files/metafile paths. Package/configuration inputs, installed `yaml` 2.9.0, lockfile, workflow and TypeScript configuration match controlled mirrors. Six externals are runtime builtins.
- Retained repeat outputs rehash correctly; both repetitions per toolchain agree. Adopted bundles match Bun 1.3.12. Pi matches HEAD; Codex control is unchanged. No manual-edit discrepancy was found.
- Lexical review confirms 71 wrapper transformations per bundle with zero wrapper lexical hazards, export-order-only normalization, no unclassified unminified changes and minified-body binding. Dynamic probes prove source/generated behavior: invalid recall arguments, token/body separation, multibyte receipt hashes, session/turn/generation metadata and Pi hooks.
- Official release/download integrity and executing Bun agree. The guard checks the workflow pin before writes and invokes the same absolute child. Matching generation, mismatch-zero-mutation and fake-PATH evidence is sound.
- Installer/profile code consumes packaged generated bytes. Query-only recall, advisory authority, provider-neutral screening and metadata-only acknowledgment remain intact.
- Lead's 228-pass Verify, focused TypeScript, four named OpenSpec validations, pinned compiled smoke and build-tooling evidence are accepted. Historical broad successes are inherited, not newly executed.
- Baseline comparison found only authorized bundles, tooling/test additions and stabilization artifact changes. No unexplained delta was found in inventoried unrelated WIP.

## Optional O-GEN-001 — Ambient Codex dependence in inherited tests

Two tests at `packages/adapter-codex/src/runner-adapter.test.ts:767,837` omit injected Codex preflight. The interrupted review's `focused.log` contains **51 pass / 2 fail**, not an all-green run.

Only those two tests were rerun under pinned Bun 1.3.12 with the same source and isolated environment:

| Only changed input | Result |
|---|---|
| PATH without Codex | Exit 1; 0 pass / 2 fail |
| Same PATH with offline version/help-only Codex fixture | Exit 0; 2 pass / 0 fail |

Both test bodies are byte-identical to HEAD. Missing Codex returns from preflight before project-config inspection. This is an inherited environmental dependency, not a demonstrated GEN-001/Bun regression. Optional follow-up: inject supported preflight while retaining project-snapshot inspection. No repair was performed; no Required repair/re-review cycle was consumed. The raw failed attempt and causal diagnostic are preserved under `gen-001-evidence/quality/`; originals remain at `/tmp/opencode/gen-001-quality/`.

## Readiness and limits

Quality conditionally approves Archive of the three named ready changes after Lead persists this verdict and READY. Existing capture-policy O1 (uneven negative-precedence test combinations), heuristic/privacy limits, prior full-suite response-loss deviation, failed reviews and mutation proofs remain historical evidence. None is converted to global green.

Historical OpenCode `ab7a...` bytes remain unavailable and outside the new equivalence claim. Non-host targets were compile-only. No live provider, canary acceptance, or universally hermetic clean-CI suite is claimed. Quality modified no candidate, OpenSpec, generated or Git state.
