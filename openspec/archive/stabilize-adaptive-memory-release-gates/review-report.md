# Review Report: Stabilize Adaptive Memory Release Gates

## GEN-001 expanded-subject Review boundary — 2026-09-07

Fresh independent `deck-quality` Review returned **GO**, zero Critical/Required and one new Optional, for the nine-path subject. See [gen-001-review.md](gen-001-review.md). The previous five-path GO is explicitly **not valid for the new generated/tooling bytes**. Historical failed Review, repair and re-review below remain intact; the new GO is independently bound, not inferred from them.

## Initial independent Review

- Reviewer: `deck-quality`
- Verdict: **NO-GO**
- Critical: 0
- Required: 1
- Optional: 1

### Required — complete bounded mounted TUI transitions

REQ-STAB-005 remained incomplete because direct `waitUntilRenderFlush()` calls at `apps/cli/src/tui/app.codex-discovery.test.tsx:168,174,254,260,263,268,272,287` bypassed the new bounded helper. A stalled render could still reach Bun's generic 30-second timeout without deterministic diagnostics or cleanup.

Minimum repair: route every direct render flush through a labeled bounded helper, retain mutation coverage for a never-resolving flush, rerun only the affected TUI test and `git diff --check`, refresh focused Verify, and perform one independent re-review. The other four focused groups remain valid.

### Optional — stale Apply status sentence

`apply-progress.md` still described implementation evidence as pending despite the completed evidence and Review state. Lead corrected the lifecycle sentence while opening the repair incident.

## Approved findings

- Bundle baseline update is history-backed and does not hide a production regression.
- Core secret screening is provider-neutral, no weaker for the removed header, and appropriately bounded to high-confidence forms.
- RunnerAdapter injected coverage proves fixed argv, floored timeout, bounded output, and timeout classification; default subprocess kill behavior remains source-reviewed rather than execution-tested.
- Ownership and unrelated-WIP preservation were correct.

## Re-review

- Reviewer: `deck-quality`
- Verdict: **GO**
- Critical: 0
- Required: 0
- Optional: 2 lifecycle-readability notes, both reconciled by Lead without changing implementation.

The original Required finding is closed. Every former direct mounted render-flush bypass now uses the labeled bounded helper; causal discovery signals, bounded cleanup, and the never-resolving mutation proof remain. The candidate may proceed to the single mandatory BROAD gate, but this GO does not itself authorize Archive or release activity.

Residual risks carried into BROAD are full-suite/release-command behavior, final dirty-worktree inventory, heuristic limits of high-confidence secret screening, and source-reviewed rather than execution-tested default subprocess kill behavior.

## Post-BROAD evidence boundary

The independent GO above is unchanged and limited to the five reviewed implementation/test paths. Final reconciliation discovered GEN-001: canonical generation changed the OpenCode generated bytes and introduced a Pi generated delta. No independent judgment on those new bytes was issued. See `release-readiness.md`; final release readiness and Archive remain blocked without inventing a new Review verdict.
