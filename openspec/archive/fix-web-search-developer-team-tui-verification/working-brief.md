# Fix Web Search Developer Team TUI verification

## Intent and authority

Resolve the sole Required blocking Deck v0.4.0 preparation. The user authorizes one causal TDD repair, focused Verify, TypeScript, exactly one new complete `bun test`, independent Review, conditional Archive, and append-only release-preparation evidence plus an updated exact commit manifest. No staging, commits, branch creation, push, tags, publication, live providers, version/date changes, unrelated WIP changes, or manual generated edits.

## Acceptance

- The dashboard Web Search materialization test MUST pass with pinned Bun 1.3.12, Node, and a controlled CI-like PATH without Codex, OpenCode, Deck or personal tools.
- Diagnosis MUST demonstrate the exact failing stage and distinguish production defects from an incomplete fixture, environment dependency, race, stale expectation, timer-only wait, or shared state.
- The test MUST traverse actual dashboard install/materialization. Provider-neutral Web Search instructions MUST exist for Developer Team roles, differ appropriately by role, and MUST NOT leak to unrelated roles/skills.
- Preserve all existing contractual assertions and strengthen negative coverage if necessary; do not fabricate expected output files.
- Use bounded causal signals for asynchronous work. Do not increase timeouts, add sleeps/blind retries, skip tests, reduce coverage, or depend on real external tools.
- Root version remains 0.4.0 and changelog release date remains 2026-09-07. Four existing archives remain closed and byte-identical.
- Test-only work reuses prior passing descriptor/helper/benchmark/compiled/build/canonical OpenSpec gates; production changes require an explicit causal gate-impact analysis before proceeding.

## Current reproduction and trace

HEAD: `dd596899b649ad9a0cda00b1d85749f8a281bab5`; branch/upstream `main` / `origin/main`; staging empty. Initial inventory: `/tmp/opencode/release-v0.4.0-tui-20260907/intake.json` and `.diff`, 2,006 tracked/untracked paths plus generated hashes. The previous release attempt and its Required remain preserved.

The original test in `apps/cli/src/tui/app.web-search-developer-team.test.tsx` fails alone under the CI-like environment: exit 1, timeout waiting for OpenCode Developer Team files at the existing lines 54/107. Full evidence: `red-individual.log`, `.started.json`, `.running.json`, `.result.json` in the same temporary directory. The file currently has only one test, so a second pre-repair whole-file run would add no selection evidence.

Its helper deadlines currently use 8,000 ms and its file timeout 20,000 ms, whereas the user requests no increase beyond five seconds. New causal waits MUST use at most 5,000 ms; do not increase any existing test budget. This discrepancy does not justify extending a timeout.

Known diagnostic entrypoints, not established causes: review-plan `plan.ready`, plan revision, operation identity, dashboard install actions, the real adapter's materializer, and the current wait's final-file predicate. `getDashboardContinueEffect` can block before install; `runRunnerReviewPlan` checks a ready/current reviewed plan before any action. The previous Node and synthetic-readiness experiments did not establish the TUI root cause.

## Ownership and verification

`deck-apply-deep` owns diagnosis and implementation as one vertical candidate. Lead owns all OpenSpec and release artifacts. `deck-quality` independently reviews; Apply must not review itself.

First reproduce/diagnose, prove RED, then minimal repair and regression GREEN. Run affected test, its whole file, directly related dashboard install/Developer Team/Web Search tests, the previously affected process tests, and TypeScript. Only after all focused checks pass may Lead execute the single new full suite with persistent logs/PID state. Do not repeat a surviving process after channel response loss. Archive only after Review GO and named validation with zero errors/warnings.

## Protected bytes and boundaries

Preserve `.serena/project.yml`, both user-protected documentation files, the prior same-turn Working Brief WIP, all other intake files outside the causal repair, all four closed archives, root package/CHANGELOG/lock, and all generated assets. Default to test-only changes. Production edits require demonstrated necessity and the smallest corresponding boundary; notify Lead with evidence and causal gate implications before expanding beyond the known test.

## Diagnosis and implementation result

An empty tools review made unrelated default-selected tools appear missing and inserted automatic provisioning before Web Search config/team apply. The real materializer was not reached. Apply repaired only test fixture evidence and causal synchronization, preserving the real effects and all role-policy assertions; an isolated old-fixture mutation fails before Install. See `apply-progress.md` for exact source evidence and RED/GREEN. Lead focused verification and TypeScript are green; the single authorized full suite is running as recorded in `verify-report.md`.

The single authorized suite has now completed: 4,829 pass, one pre-existing skip, zero failures. Verify GO; independent Review is the remaining change-closure gate.
