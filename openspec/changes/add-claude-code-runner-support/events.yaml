schema: spec-registry-events-v1
events:
  - phase: explore
    status: completed
    event: explore.completed
    artifact: proposal.md
    timestamp: "2026-08-29"
    actor: claude-code-session
    notes:
      - "Inspected the RunnerAdapter contract, the OpenCode and Codex reference adapters, and Anthropic's Claude Code headless-mode / MCP / Agent SDK documentation."
  - phase: proposal
    status: completed
    event: proposal.completed
    artifact: proposal.md
    timestamp: "2026-08-29"
    actor: claude-code-session
    registry_write: non-deferred
    notes:
      - "Scoped a staged Claude Code adapter modeled on the Codex precedent; decided subprocess over Agent SDK."
  - phase: spec
    status: completed
    event: spec.completed
    artifact: spec.md
    timestamp: "2026-08-29"
    actor: claude-code-session
    registry_write: non-deferred
    notes:
      - "39 RFC 2119 requirements across composition, compatibility, launch safety, materialization, instruction translation, models, and documentation honesty, plus 9 acceptance scenarios."
  - phase: design
    status: completed
    event: design.completed
    artifact: design.md
    timestamp: "2026-08-29"
    actor: claude-code-session
    registry_write: non-deferred
    notes:
      - "Recorded the subprocess-vs-Agent-SDK decision, reused the existing launch contract unmodified, and flagged the stdout-vs-file output-capture gap against Codex's precedent as an open Phase 0 decision."
  - phase: tasks
    status: completed
    event: tasks.completed
    artifact: tasks.md
    timestamp: "2026-08-29"
    actor: claude-code-session
    registry_write: non-deferred
    notes:
      - "Six phases: compatibility spikes, minimal composition, launch, Developer Team materialization, models/catalog/doctor, documentation and hardening."
  - phase: tasks
    status: completed
    event: tasks.completed
    artifact: tasks.md
    timestamp: "2026-08-29"
    actor: general-purpose-review-agent
    registry_write: non-deferred
    notes:
      - "Independent line-by-line review against real source. Verdict: APPROVE WITH NOTES."
      - "Applied 3 corrections: explicit RunnerAdapter stub-implementation task in Phase 1, explicit --model pass-through in Phase 2, fixed a requirement-id citation in design.md."
  - phase: tasks
    status: completed
    event: tasks.completed
    artifact: tasks.md
    timestamp: "2026-08-29"
    actor: claude-code-session
    registry_write: non-deferred
    notes:
      - "Phase 0 (compatibility and safety spikes) completed live against authenticated claude v2.1.251: real --help fixture captured, launch-policy token decided (--permission-mode bypassPermissions, not the name-similar --dangerously-skip-permissions which an undocumented auto-mode classifier blocks), and the output-capture design question settled (stdout JSON, no file channel)."
  - phase: tasks
    status: completed
    event: tasks.completed
    artifact: tasks.md
    timestamp: "2026-08-29"
    actor: claude-code-session
    registry_write: non-deferred
    notes:
      - "Phase 1 (minimal composition and detection) complete: @deck/adapter-claude scaffolded and registered; real detectRuntimes/inspectProject; every non-optional RunnerAdapter method implemented (real or phase-labeled throwing stub)."
      - "Verified via stash-based before/after comparison: 29/29 new tests pass; full suite's failing-test-name set is byte-for-byte identical pre/post (zero regressions); tsc clean for all touched files."
  - phase: tasks
    status: completed
    event: tasks.completed
    artifact: tasks.md
    timestamp: "2026-08-29"
    actor: claude-code-session
    registry_write: non-deferred
    notes:
      - "Phase 2 (launch) complete: buildClaudeLaunchPlan for all four modes, wired into ClaudeRunnerAdapter.buildLaunchPlan."
      - "Two real design corrections caught during implementation: output-capture needed a new source:\"stdout\" variant on the shared RunnerLaunchPlan type (Codex's file-based contract doesn't apply — Claude has no --output-last-message equivalent); preflight.ts was deriving compatibility from a static fixture lookup instead of the live probed help text, fixed to match Codex's precedent."
      - "Verified via stash-based before/after full-suite diff (diff exit 0 on failing-test names): zero regressions, including in the shared runner-launch-command.ts/core/runner-adapter.ts this phase touched."
  - phase: tasks
    status: completed
    event: tasks.completed
    artifact: tasks.md
    timestamp: "2026-08-29"
    actor: claude-code-session
    registry_write: non-deferred
    notes:
      - "Phase 3 (Developer Team materialization) complete: 7 roles to .claude/agents/*.md + matching .claude/skills/*/SKILL.md (reusing the shared buildDeveloperTeamManifest core function directly), CLAUDE.md marker-span merge, .mcp.json single-server safe writer, instruction translation with an integration-tested validator, and backup/apply/rollback/verify (in-memory, deliberately smaller in scope than Codex's persistent journal)."
      - "Standalone/bootstrap skills explicitly deferred as new Task 3.6, not silently dropped."
      - "96/96 package tests pass; full suite (4715/24/3, 314 files) matches the known-good baseline exactly; tsc clean. This phase touched zero files outside packages/adapter-claude."
  - phase: tasks
    status: completed
    event: tasks.completed
    artifact: tasks.md
    timestamp: "2026-08-29"
    actor: claude-code-session
    registry_write: non-deferred
    notes:
      - "Phase 4 (models, capability catalog, doctor) complete. Found and fixed a real bug inherited from Phase 2/3: canonical model catalog IDs 404 against the real Claude Code binary; only sonnet/opus/haiku aliases work. Fixed with an explicit mapping table wired into both launch and materialized frontmatter."
      - "Also found and wired --effort <level> (low/medium/high/xhigh/max), missed in Phase 0's condensed excerpt."
      - "readModelAssignments/readThinkingAssignments, capability catalog with honest gaps (Task 4.2), and real deck doctor diagnostics (Task 4.3) all implemented."
      - "117/117 package tests pass; full suite (4736/24/3, 315 files) matches the known-good baseline exactly; tsc clean. A live end-to-end run against the real claude binary confirmed the model-mapping fix and --effort together."
  - phase: tasks
    status: completed
    event: tasks.completed
    artifact: tasks.md
    timestamp: "2026-08-29"
    actor: claude-code-session
    registry_write: non-deferred
    notes:
      - "Phase 5 (CLI command surface) complete -- added outside the original plan after testing surfaced no deck claude developer command existed at all. parseClaudeArgs added to cli-args.ts; main.tsx's dispatch needed zero changes (already generic)."
      - "Found and fixed a real bug that would have blocked every apply forever: buildClaudeDeveloperTeamInstallPlan never populated the optional mutationPreview field, which apps/cli's runner-launch-command.ts requires to allow apply at all. Fixed with real SHA256-hash mutation entries mirroring Codex's construction."
      - "Recorded a testing-process near-miss: bun run --cwd apps/cli caused resolveProjectRoot() to resolve to the real repo checkout itself, briefly writing untracked files into it -- caught via git status same-turn, removed safely."
      - "Verified end-to-end through the real deck binary against the real authenticated claude process: dry-run, install-only apply, and exec -- \"prompt\" all confirmed working (is_error: false)."
      - "cli-args.test.ts 59/59 pass; adapter-claude 120/120 pass; full suite (4742/24/3, 315 files) diff against a stash-based baseline is byte-for-byte identical; tsc clean."
  - phase: tasks
    status: completed
    event: tasks.completed
    artifact: tasks.md
    timestamp: "2026-08-29"
    actor: claude-code-session
    registry_write: non-deferred
    notes:
      - "Phase 6 (interactive TUI dashboard integration) complete -- added on explicit user request to make Claude selectable/installable through deck's interactive menu, not just the direct CLI command."
      - "Found the menu label was already real (buildEnvironmentMenuOptions overwrites the static placeholder for any registered adapter); the actual blocker was reviewTools() throwing, caught by composeRegisteredRunnerDashboard, showing a generic error."
      - "Repo-wide grep showed only buildReviewPlan and reviewTools have real production call sites; buildInstallationPlan/getNextScreen are unused, runAction only fires for capabilityId==='serena' which Claude's catalog never selects. Implemented all five for real; removed the now-fully-dead notYetImplemented() helper -- every RunnerAdapter method is real."
      - "Verified by reproducing composeRegisteredRunnerDashboard's exact call sequence plus the full installTeamBundle apply sequence against a real scratch project: succeeded without throwing, ready:true, 15-file install. adapter-claude 120/120 pass; full suite (4742/24/3, 315 files) matches the known baseline exactly; tsc clean."
